/** Serializable types shared with client components (re-exported so client bundles don't import the server-side report builder). */
export type { CampaignWowRow, AdWowRow, TrendPoint, ReportMetricKey } from "@/lib/reports/weekly";
export type { Platform } from "@/types/domain";
